<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.harjoitustyo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CardInfoActivity"></activity>
        <activity android:name=".CardPaymentActivity" />
        <activity android:name=".WireTransferActivity" />
        <activity android:name=".UserSettingsActivity" />
        <activity android:name=".OwnTransferActivity" />
        <activity
            android:name=".AccountInfoActivity"
            android:noHistory="true" />
        <activity
            android:name=".AccountsActivity"
            android:noHistory="true" />
        <activity
            android:name=".NewUserActivity"
            android:noHistory="true" />
        <activity
            android:name=".LoginActivity"
            android:noHistory="true">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:noHistory="true"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>